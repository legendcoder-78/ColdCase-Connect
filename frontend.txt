Act as an Expert UI/UX Developer. I am expanding the frontend for "ColdCase Connect" (a forensic intelligence platform). 

CURRENT PROJECT STATE:
- My main application dashboard already exists in a file named `index.html`. 
- DO NOT MODIFY `index.html` under any circumstances.
- DO NOT TOUCH ANY PYTHON/BACKEND FILES. 

YOUR TASK:
Create two entirely new, static files: `landing.html` and `login.html`. 
Use ONLY Vanilla HTML5, CSS3, and JavaScript. Keep all CSS/JS embedded within the respective HTML files for simplicity.

--- ### DESIGN SYSTEM: "MIDNIGHT TACTICAL" ### ---
- Background: Deep Charcoal/Navy-Black (#0B0E14)
- Primary Accent: Electric Cyan (#00D2FF) for active states/glows.
- Font: Clean sans-serif for body, Monospace for data/tags.
- UI Elements: Subtle borders (1px solid #2D343F), glassmorphism, and thin-stroke icons.

--- ### PAGE 1: landing.html (The Front Door) ### ---
1. HERO SECTION:
   - Full-screen HTML5 Canvas background animation: "Connecting Nodes" (white/cyan dots drifting slowly, drawing thin lines when close).
   - Main Heading: "ColdCase Connect" (Glowing cyan).
   - Subtitle: "AI-Powered Multi-Modal Cold Case Intelligence Platform".
   - Primary Button: "Launch Investigation" -> MUST link to `login.html`.
2. VISUAL TAGLINE SECTION:
   - Flexbox of glowing tactical cards: [Semantic Intelligence], [Evidence Matching], [Pattern Correlation], [Divergence Detection].
3. CORE FEATURES (CSS Grid of Glassmorphism Cards):
   - Multi-Modal Vector Search: Fuses textual and visual evidence.
   - Best Evidence Fusion: Aggregates max scores from multi-image uploads.
   - Divergence Detection: Flags inconsistencies between M.O. and visuals.
4. TECH STACK (Monospace Badges):
   - [FastAPI] [Python] [Gemini 2.5 Flash] [ChromaDB] [Vanilla JS]

--- ### PAGE 2: login.html (The Auth Portal) ### ---
1. AESTHETIC: Highly secure, centered terminal login box.
2. TITLE: "RESTRICTED ACCESS: COLDCASE CONNECT"
3. INPUTS: "Secret Identity Username" (text) and "Department ID" (password). Styled like terminal prompts.
4. ACTION BUTTON: "Authenticate & Access Database".
5. JAVASCRIPT ROUTING LOGIC (CRITICAL - KEEP IT FAKE):
   - You can keep the authorization logic fake as of now. Do not write any backend auth code. 
   - Add a simple event listener to the button. 
   - On click, change the button text to "Verifying Credentials..." and add a blinking cursor effect.
   - Wait exactly 1500ms using `setTimeout()`.
   - Execute routing: `window.location.href = 'index.html';` 

Please output the complete, ready-to-run code for `landing.html` and `login.html`.