Act as a Full-Stack Developer. My ColdSync AI dashboard is failing to render 'Archived Match' images (they show broken icons), although the 'Input Evidence' renders fine. 

I need to fix the Static File Serving between FastAPI and the HTML/JS frontend.

Requirements:

1. Update main.py:
   - Import 'StaticFiles' from 'fastapi.staticfiles'.
   - Add 'app.mount("/train", StaticFiles(directory="train"), name="train")' immediately after the CORS middleware. This will allow the browser to access images via 'http://127.0.0.1:8000/train/...'.

2. Update the /analyze Endpoint in main.py:
   - Ensure the 'image_path' returned in the JSON results is relative to the project root (e.g., "train/street/image_01.jpg"). 

3. Update the JavaScript (script.js or index.html):
   - Locate where the 'Archived Match' <img> tag is created.
   - Update the 'src' attribute so it prefixes the backend URL. 
   - Example: imgElement.src = `http://127.0.0.1:8000/${match.image_path}`;

Please provide the specific code blocks for main.py and the JavaScript rendering logic to ensure these archived images appear correctly.