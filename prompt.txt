Act as a Full-Stack Engineer. I want to upgrade my ColdSync AI project to support "Multi-Image Context Matching." 

Currently, the user uploads 1 text description and 1 image. I want them to be able to upload 1 text description and MULTIPLE images. The system should calculate the "Best Visual Match" out of the uploaded batch for each archived case.

Please provide the updated code for both the backend (main.py) and the frontend.

Requirements for Backend (main.py):
1. Endpoint Signature: Update the '/analyze' endpoint to accept multiple files: `files: List[UploadFile] = File(...)`.
2. Multi-Embedding Extraction: Loop through the uploaded 'files', convert each to RGB, and generate a CLIP visual embedding for each one.
3. ChromaDB Batch Query: Pass the list of all generated visual embeddings into ChromaDB's 'collection.query(query_embeddings=[...])' simultaneously.
4. "Best of N" Aggregation Logic (CRITICAL): 
   - ChromaDB will return results for EACH uploaded image.
   - You must aggregate these results. Group them by the archived 'Case ID' (or database ID).
   - For each archived case, find the MAXIMUM visual similarity score across all the uploaded images.
   - Use this MAX visual score to calculate the final 'combined_score' and 'divergence_flag'.
   - Return the standard JSON response, sorted by the highest combined score.

Requirements for Frontend (HTML/JS):
1. HTML: Update the file input element to include the 'multiple' attribute so the user can select multiple files from their OS.
2. JavaScript: Update the 'FormData' appending logic. Instead of appending a single file, iterate through 'fileInput.files' and append each one to the 'files' key so FastAPI receives the list properly.

Output the specific Python and JavaScript code blocks required to implement this.